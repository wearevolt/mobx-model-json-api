import { expect } from 'chai';

describe('BaseModel', () => {

  beforeEach(function () {
  });

  afterEach(function() {
  });

  it("should be true", function() {
    expect(true).to.equal(true);
  });

});
